{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
  },
  "scripts": {
    "api": "python -m  uvicorn cs-mansion-api:app --host 192.168.1.104 --port 8000  --reload",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  }
}
